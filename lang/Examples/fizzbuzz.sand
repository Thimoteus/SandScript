(load "lang/Prelude.sand")

(def numbers (range 1 100))

(def (fizzbuzz n)
  (cond
    ((divides 15 n) 'fizzbuzz)
    ((divides 3 n) 'fizz)
    ((divides 5 n) 'buzz)
    (else n)))

(map fizzbuzz numbers)
